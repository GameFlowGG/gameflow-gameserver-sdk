packages:
  - sdk/typescript
  - sdk/typescript/examples/node-tcp
  # sdk/typescript/examples/colyseus is intentionally excluded: it is npm-managed
  # (package-lock.json) because GameFlow's Colyseus build template runs npm ci.
