@tool
extends EditorPlugin

# The SDK is plain GDScript exposed through class_name (GameFlow,
# GameFlowResult, ...), so there is nothing to register here. The plugin
# exists so the addon shows up in Project Settings and the Asset Library.
